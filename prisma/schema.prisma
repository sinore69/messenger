generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

model FriendRequest {
  id      Int    @id @default(sequence())
  from_id String
  to_id   String
}
